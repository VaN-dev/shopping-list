<div class="c-panel widget">
    <div class="c-panel__actions">
        <a href="{{ path('app.friend.request', {'id': user.id}) }}" class="btn btn-sm btn-default"><i class="zmdi zmdi-account-add"></i></a>
    </div>
    <img class="widget__img widget__img--responsive widget__img--circle" src="{{ user.avatar }}" alt="">
    <div class="c-panel__body widget__body">
        <p>
            <a href="{{ path('app.user.details', {'id': user.id}) }}">{{ user.username }}</a>
        </p>
        <p class="text-muted">4 recettes visibles</p>
    </div>
</div>
